<form {{action 'addPost' on='submit'}}>
  <div class="form-group">
    <label>Title of Post</label>
    {{input value=title placeholder='Title' class='form-control'}}
  </div>
  <div class="form-group">
    <label>Subtitle</label>
    {{input value=subtitle placeholder='Subtitle' class='form-control'}}
  </div>
  <div class="form-group">
    <label>Upload an Image</label>
    {{input value=image placeholder='Paste image URL here' class='form-control'}}
  </div>
  <div class="form-group">
    <label>Content</label>
    {{input value=content2 placeholder='Content' class='form-control'}}
  </div>
  <div class="form-group">
    <label style="padding-right:20px;vertical-align:top;">Tags</label>
    {{!view "select" value=tags2 content=tags optionValuePath="content.id" optionLabelPath="content.name"}}
    {{view Ember.Select contentBinding=tags valueBinding=tags2 optionValuePath="content.name" optionLabelPath="content.name"}}
    {{!input type='checkbox' checked=tag.name class='form-control'}}
    {{!#each tags2 as |tag|}}
    <!--<input type="checkbox" name="tags" value={{tag.name}}> {{tag.name}}<br>-->
    {{!/each}}
  </div>
  <button type="submit" class="btn btn-default">Create Post</button>
</form>
